# Release Checklist

- [ ] `zeus plugin check . --registry --json` passes
- [ ] `metadata.json` and manifest versions match
- [ ] Entry files exist for each component
- [ ] Install/config example payloads are valid JSON
- [ ] `index.json` contains the plugin with correct paths
