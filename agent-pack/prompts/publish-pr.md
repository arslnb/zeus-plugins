Prepare a plugin submission PR for this registry repository.

Include:
1) Plugin files under `plugins/<plugin_id>/`
2) Updated `index.json`
3) PR summary with capability, required secrets, and validation command output.
