{
  "schema_version": 1,
  "id": "gws",
  "name": "Google Workspace",
  "version": "0.1.1",
  "description": "Run Google Workspace operations through the gws CLI, including Gmail inbox triage.",
  "components": {
    "client": {
      "runtime": "python",
      "entry": "client/plugin.py"
    },
    "server": {
      "runtime": "python",
      "entry": "server/plugin.py",
      "channels": []
    }
  },
  "config_schema": {
    "client": {
      "gws_binary": {
        "type": "string",
        "secret": false,
        "required": false,
        "description": "Path or executable name for the Google Workspace CLI.",
        "default": "gws"
      },
      "google_client_id": {
        "type": "string",
        "secret": true,
        "required": false,
        "description": "Optional desktop OAuth client ID for the Google Workspace CLI. Leave empty to reuse any existing local gws client config on this Mac."
      },
      "google_client_secret": {
        "type": "string",
        "secret": true,
        "required": false,
        "description": "Optional desktop OAuth client secret paired with the client ID above. Zeus stores it in Secret Vault and injects it only when provided."
      },
      "default_user_id": {
        "type": "string",
        "secret": false,
        "required": false,
        "description": "Default Gmail user id for mailbox operations.",
        "default": "me"
      },
      "default_query": {
        "type": "string",
        "secret": false,
        "required": false,
        "description": "Default Gmail query for unread message triage.",
        "default": "in:inbox is:unread"
      },
      "default_max_results": {
        "type": "integer",
        "secret": false,
        "required": false,
        "description": "Default Gmail unread list size (1..25).",
        "default": 5
      },
      "timeout_seconds": {
        "type": "integer",
        "secret": false,
        "required": false,
        "description": "Timeout for gws subprocess calls.",
        "default": 45
      }
    },
    "server": {}
  },
  "secret_storage": "vault",
  "prerequisites": {
    "agent_guidance": "Install the required CLIs one prerequisite at a time. Auto-run only the manifest-declared shell commands. If a step needs manual work, explain why, open the install docs, and stop after the prerequisite verifies successfully.",
    "cli": [
      {
        "id": "google_workspace_cli",
        "name": "Google Workspace CLI",
        "binary": "gws",
        "required": true,
        "why": "This plugin executes Gmail and other Google Workspace actions through the gws CLI.",
        "check_command": "gws --version",
        "docs_url": "https://github.com/googleworkspace/cli",
        "install_options": [
          {
            "type": "shell",
            "label": "Install with npm",
            "description": "Installs the Google Workspace CLI globally with npm.",
            "command": "npm install -g @googleworkspace/cli",
            "auto_run": true,
            "requires_admin": false,
            "platforms": ["darwin", "linux"]
          },
          {
            "type": "open_url",
            "label": "Open install docs",
            "description": "Use this if npm is unavailable or the automatic install fails.",
            "url": "https://github.com/googleworkspace/cli"
          }
        ]
      }
    ]
  },
  "oauth": {
    "mode": "cli_sequence",
    "description": "Runs the gws browser auth flow through the Zeus install handoff.",
    "login_command": "gws auth login",
    "interactive": true,
    "success_check_command": "gws auth status",
    "secret_env": {
      "GOOGLE_WORKSPACE_CLI_CLIENT_ID": "client.google_client_id",
      "GOOGLE_WORKSPACE_CLI_CLIENT_SECRET": "client.google_client_secret"
    },
    "agent_prompt": "Run gws auth login in an interactive process session. If the Install form stored Google OAuth client credentials, Zeus already provides them from Secret Vault; otherwise gws should use any existing local client config on this Mac. Stream the output back to the user. If gws opens a browser or prints a consent URL, show it plainly and wait for the user to finish. After the flow, verify success with gws auth status."
  },
  "skills": {
    "bundled": true,
    "path": "skills",
    "auto_install": true,
    "auto_enable": true,
    "uninstall_policy": "remove"
  }
}
